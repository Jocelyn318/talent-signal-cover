# 同事部署与使用说明

适用仓库：<https://github.com/Jocelyn318/talent-signal-cover>。仓库现已公开，任何人都可浏览和下载。这仍是一个本地工作包，不是多人在线网站。

## 1. 获取公开仓库

直接打开仓库即可阅读，下载或 HTTPS 克隆无需协作邀请，也通常无需登录。点击 Code → Download ZIP 可下载完整包，或使用下方 git clone 命令。只使用 Skill 和编辑器不需要仓库写入权限。

公开不意味着所有人都能直接修改仓库。希望贡献变更时，登录自己的 GitHub 账号，Fork 后在分支上修改并提交 Pull Request；已有写权限的同事也请用分支和 Pull Request，由维护者审阅，不直接改 main。

## 2. 你需要什么

|用途|依赖|
|理解内容、提案与生图|支持本地 Skill 的 Codex 环境，以及可用的图像生成／编辑工具|
|本地文字编辑器|Python 3、现代桌面浏览器|
|克隆和更新仓库|Git；公开 HTTPS 克隆无需协作邀请，也可从仓库 Code 菜单下载 ZIP|
|可选的项目生成脚本和 JS 测试|Node.js 18 或更高版本|

仓库不带模型、API 密钥或图像生成额度；安装 Skill 不会自动给账号开通生图能力。编辑器本身不调用 AI，只有导入背景和文字排版功能。字体已随包，无须 npm install 或连接外部字体网站。

## 3. 安装 Skill（推荐让 Codex 完成）

在你自己的 Codex 中发送：

```text
使用 skill-installer，将以下公开仓库
https://github.com/Jocelyn318/talent-signal-cover
的根目录安装为 talent-signal-cover。
请安装完整目录，包括 references、assets、editor 和 scripts。
若同名技能已存在，先告诉我，不要直接覆盖。
```

公开仓库通常可直接下载安装，无需提供 GitHub 密码或令牌。不要把凭据放进项目文件；若工具因网络或限流要求认证，使用你自己的认证流程，不把凭据发给同事。如果当前环境没有 skill-installer，可让管理员按客户端的本地 Skill 加载方式安装整个目录；只复制 SKILL.md 会丢失参考图片和字体。

安装后必须检查 `references/images` 中有 15 张参考、`assets/examples` 中有两张样例，以及 `editor/fonts`、`scripts` 均存在。部分安装器在对仓库根目录执行 Git 稀疏检出时只复制顶层文件，即使显示安装成功也可能缺资源。若安装目录保留 `.git`，可让 Codex 在该目录执行 `git sparse-checkout disable` 补齐，再检查；否则从完整克隆或 ZIP 补齐，不覆盖用户自定义内容。

安装完成后在下一轮对话使用 `$talent-signal-cover`。如果界面尚未刷新，重新打开会话或客户端后检查；仍不显示时让 Codex 检查实际安装目录是否有 SKILL.md。官方背景资料：[创建与使用 Skills](https://learn.chatgpt.com/docs/build-skills)。

## 4. 启动编辑器

### 获取工作副本

已安装 Skill 的用户可让 Codex“打开 talent-signal-cover 配套编辑器”，它会定位安装目录并启动服务。

也可以另行下载仓库 ZIP 并解压，或在已安装 Git 的终端运行（公开 HTTPS 克隆通常无需认证）：

```sh
git clone https://github.com/Jocelyn318/talent-signal-cover.git
cd talent-signal-cover
```

下面所有命令都在包含 SKILL.md 和 scripts 的仓库根目录运行，不要在上一级文件夹运行。

### macOS / Linux

```sh
python3 scripts/serve.py
```

### Windows

```powershell
py -3 scripts/serve.py
```

浏览器打开终端显示的地址，默认 <http://127.0.0.1:8765/>。保持终端运行，Ctrl+C 会停止服务；重新使用时再次启动。这个链接只指向你自己的电脑，不能把它发给同事当在线编辑器链接。

如果 8765 被占用，macOS / Linux 运行 `python3 scripts/serve.py --port 8766`，Windows 运行 `py -3 scripts/serve.py --port 8766`，然后打开 <http://127.0.0.1:8766/>。不会关闭或接管已占用端口的服务。

发行包入口是 `/`，不是历史开发版的 `/editor/`。不建议直接双击 editor/index.html，以免本地文件协议影响字体加载。请等界面显示“字体已就绪”。

## 5. 一次封面的完整流程

### A. 提交内容，先看提案

```text
使用 $talent-signal-cover。
下面是本期文章：[粘贴正文]
先理解内容，给我封面创意和视觉参考说明，确认前不要生图。
新参考只借元素与关系，不沿用背景配色。
```

提案包含：核心观点、建议标题／副标题／强调词、画面隐喻、构图和留白、元素参考、单独的配色来源。看清方案后回复“确认 v1 的 A 方案，生成一张无字 demo”。确认具体版本，不笼统让模型自行选。

### B. 确认后生成无字背景并反馈

默认无标题、副标题、栏目名或水印。当前审美是明亮、柔和的未来复古摄影拼贴，优先灰青蓝＋暖米白，不把地图、环带或手势固定成模板。

反馈尽量指出维度，例如：“保留构图和人物，只把背景换成更柔和的灰青蓝”；“颜色合适，但隐喻像控制别人，重新提案”。局部改图保留确认项，改主概念先重新确认。快稿可以直接要求“单张 demo，不精修”。

### C. 在编辑器排字

1. 下载无字背景，在编辑器点击“上传／替换背景图”。
2. 分别修改主标题、副标题、栏目名；输入标题不会重生背景。
3. 栏目名初始 64 px 且默认隐藏，需要时切到栏目名并勾选“显示图层”。标题和副标题初始为空。
4. 调整换行、字号、位置、颜色和强调词。人工编辑自动启用“防 AI 覆盖”，但不妨碍你继续手动编辑。
5. 分别检查 9:16 与 3:4 预览。3:4 背景放进 9:16 母版会左右裁切，应调整焦点，必要时回到生图阶段处理画幅，不靠裁切凭空补图。
6. 导出 PNG：9:16 为 1080×1920；中央 3:4 为 1080×1440。
7. 点击“保存项目”下载 JSON。务必在关页前保存，没有自动保存。

### D. 下次继续与同事交接

打开编辑器后用“打开项目”导入 JSON，可恢复背景和三个文字层。交接时发送 JSON（可继续编辑）及 PNG（成品），仅有 PNG 不能恢复文字层。JSON 内嵌背景，可能包含本期未发布内容，请走团队认可的分享渠道，不默认提交到本仓库。

## 6. 更新与协作约定

- 用户作品、原始正文、临时生成图放在独立工作文件夹，不混入 Skill 规范或安装目录。
- 仓库使用者要更新 Git 工作副本时，先 `git status` 确认本地没有需要保留的修改，再 `git pull --ff-only`；冲突时停止，不强制覆盖。
- 通过安装器复制安装的 Skill 不一定自动随仓库更新。请求 Codex 更新时先备份本地定制、比较变更，再替换；不要直接删整个技能目录。
- 新风格偏好先记录认可范围，经维护者确认后再改规范并提 PR。单张的“好看”不等于所有元素成为品牌规则。
- 仓库公开不代表第三方参考图片已获开源、商业使用或再分发许可；使用和传播前请核实权利，见 [素材说明](ASSET-NOTICE.md)。不要把未发布正文、客户资料、密码或 API 密钥提交到公开仓库。仓库公开不会自动部署编辑器网站。

## 7. 常见问题

|问题|处理|
|仓库打不开／404|核对仓库链接、网络和仓库是否迁移；当前公开仓库不需要接受邀请|
|localhost 拒绝连接|先启动服务，检查端口和终端是否仍在运行；不要使用别人的 localhost 链接|
|打开 `/editor/` 是 404|发行版访问 `/`，以终端显示的完整链接为准|
|提示找不到 Python|安装 Python 3；Windows 尝试 `py -3`|
|字体一直未就绪|确认完整下载 editor/fonts 和 fonts.js，用 HTTP 服务打开，不要只拿 HTML|
|导出按钮灰色|先上传背景并等待字体就绪|
|改字后刷新丢失|页面没有自动保存；必须先保存项目 JSON|
|AI 没改已锁副标题|锁定层会跳过；手动修改，或明确授权解锁该层后再让 AI 更新|
|只有带字 PNG 想改标题|找回 JSON 或无字背景；不能无损恢复被压平的独立文字层|
|能看提案但无法生图|检查当前环境图像工具是否可用；Skill 不是模型或额度|

## 8. 验证与范围

可选测试：`node --test tests/*.test.cjs` 和 `python3 -m unittest discover -s tests -p 'test_*.py'`；Windows 后者可换成 `py -3`。图像生成不在这些自动化测试中，最终效果仍需要人确认。

本包已在当前 macOS 环境实测；Windows 命令按跨平台脚本给出，尚未做 Windows 实机验收。当前只支持本地静态编辑，不包含共享项目库、在线多人编辑或托管网站。
