# 发行验证

2026-09-18，初始私有发行包。

- Skill 官方 quick_validate：通过。
- 渲染核心：数据校验、字体限制、换行、强调范围、隐藏层、越界提醒测试通过。
- 新建项目：空标题／副标题、栏目名 64 px 默认隐藏、图片嵌入一致、不覆盖旧文件测试通过。
- 包检查：相对 Markdown 链接可解析，15 张参考和两张正例齐全；文档与代码没有个人绝对路径。
- 本地服务：仅回环绑定；编辑器及随包字体可访问，包外文档和目录列表不可访问。
- 浏览器实测：字体加载就绪，蓝底背景可导入，副标题修改自动锁定且主标题保持空白，3:4 PNG 导出生成下载链接。
- 独立 agent 演练：无对话上下文读取 Skill 后先提交提案等待确认，不生图；红底参考只借元素，配色采用蓝底基准。已据反馈补充锁定字段与压平图的改字边界。

本轮没有重新生成图片，不声称生成效果或每个浏览器均已回归验证。保存／重开与 9:16 像素一致性的历史验证属于旧编辑器；本包进一步使用反馈仍应记录。Skill 不包含图像模型，也不自动安装到客户端。
