# 本地验收 · 2026-09-23

已实现：统一入口、封面/正文常驻隔离模块、整组预览、浏览器自动保存、含图片/标识的整篇项目文件、旧封面JSON导入、整组ZIP和单封面导出。

## 已通过

- 原封面 render-core.js、editor.js、seed.js、fonts.js 与发行版逐字节一致；初始栏目64px，三独立文字层与锁定沿用。
- 原封面2项测试通过；统一项目验证/ZIP/原封面一致性4项测试通过；上游正文工作台3项测试通过。
- 独立无头Chrome回归：两种模式切换、旧封面入口不可见且无旧封面页面、正文修改不影响封面、封面撤销重做、锁定保留。
- 无效项目拒绝并保持原数据；旧封面JSON只更新封面；包含正文图片和自定义Logo的状态可导入。
- 自动保存后刷新恢复一致；整篇项目下载与载入一致。
- 整组导出为1封面+2正文，无重复旧封面。实测PNG尺寸：1080×1920、2160×3600、2160×3600；封面3:4为1080×1440。
- ZIP系统解压校验通过；预览使用相同渲染结果生成缩略图。
- 查看1440×1000封面/正文/整组预览及1100×780正文截图，未发现遮挡主要操作。
- Python服务语法检查通过，服务仅监听127.0.0.1:8766。

## 边界

此记录为上线前本地验收；线上发布另见仓库提交与Pages状态。未加入账号、云端同步或多人实时编辑。未做手机专项适配和超大文章压力测试。旧站点草稿不能跨域自动迁移。保留上游来源与素材许可说明。

源码适配保留了上游部分未调用的封面代码，创建封面函数无条件返回，封面导航/设置/导出均不可达；唯一正式封面渲染来自cover/原编辑器。
