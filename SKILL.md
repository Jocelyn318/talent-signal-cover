---
name: talent-signal-cover
description: 为 Talent Signal 播客或 blog 内容设计未来复古小红书封面；先围绕主题提案，再结合用户参考图及取舍确认意图，由生图子 agent 生成无字背景，支持局部改图与独立排字。不替代其它品牌风格。
---

# Talent Signal 封面

## 先读取什么

新主题或改画面：阅读 [视觉规则](references/visual-style.md)、[参考索引](references/index.md)、[内容与确认流程](references/workflow.md)。查看所选图片，不仅凭文件名推测。读取 [认可与反馈](references/approved.md) 了解正例的认可范围。

只有文字修改或打开编辑器时：读取 [编辑器说明](references/editor.md)，无需生图。路径相对本 Skill 目录；输出另存到用户工作目录，不改随包参考。

## 不可跳过的确认门槛

主 agent 先理解正文，统一设计内容与视觉，不按标题关键词机械配图。默认提交三组简短创意，已有清楚方向可只提一组。每组包含内容理解、建议标题／副标题／强调词、画面隐喻、主体关系与构图、留白、配色来源，以及实际参考与借用／排除项。

给出提案版本与方案编号，然后等待用户选择，并在生图前接收用户提供的参考图及“借什么、不要什么、为了表达什么”的说明。主 agent 实际查看参考，整理为最终视觉意图包，获得确认后才交给生图子 agent。若用户已给足参考、取舍并明确要求按此生图，不重复请求确认。仅选择方案但尚未提供参考时，先等参考，不自动生图；除非用户明确授权本次无参考直接创作。

不能为了展示提案先生成图片，也不能把最初“帮我做封面”当成已批准未展示的方案。记录版本、选项和确认原话，只执行确认范围。已有成图的明确局部修改可沿用已确认参考，直接交给生图子 agent；改变主要隐喻须重新确认。

## 生图与反馈

- 统一生成没有标题、副标题、栏目名、标志或水印的背景。提案中的文案留在项目记录，不画进图里。
- 用户新发的图片默认只借元素及其关系，不借背景配色；只有明确要求才借背景颜色。将元素参考、配色基准、编辑目标分别标注。
- 无论参考原本是摄影、插画还是其它风格，最终统一为编辑感未来复古：意象关系、摄影拼贴或套印插画、适度喷绘和温和印刷质感；不照搬参考的写实程度，不靠行星、CRT、圆环或粗重噪点堆砌风格。
- 使用可用的图像生成／编辑工具；如当前环境有 imagegen 技能，先读并遵循它。工具不可用就说明阻碍，不拿占位图当已完成生图。不承诺像素级不变或训练模型。
- 查看编辑目标和参考后再调用工具。局部修改保留已确认主体／构图；纯改字不生图；快稿只做单张 demo，不擅自精修多轮。
- 主 agent 负责内容理解、提案、参考取舍整理及最终质检；具体生成与改图交给一个生图子 agent，传递完整意图包和锁定项，不并行改同一张图。子 agent 不另起未经确认的概念。主 agent 可同时准备项目记录、文案或裁切验收。无法委派时说明限制，请用户选择等待或授权主 agent 执行，不悄悄替代。
- 默认尽量避免生成真人人脸、常规蓝紫科技感、发光电路、无意义圆环、复杂悬浮元素、满屏信息和互联网活动海报感；这不等于禁止所有人物或有明确隐喻的环形关系。详见视觉规则。
- 9:16 画布中，所有核心视觉及后续三个文字层必须完整落在正中央 3:4 安全区内。生图与排版均按视觉规则检查实际裁切；不只在提示词中写要求。
- 检查主体关系、颜色漂移、意外文字、肢体、留白与裁切；交付实际图片，不只返回提示词。存实际提示词、输入图片角色、版本和反馈。用户没认可的图不冒充正例。
- 文案、构图、配色、观感、隐喻、排版分别记录认可范围；单次意见不自动变成通用规则。用户明确要求的全局规则才更新规范。

## 交付与可编辑性

交付无字背景及项目记录。用户需要排版时再用随包 editor/：三个独立文字层，栏目名初始 64 px，粗黑体标题体系。新项目可用 `node scripts/new-project.mjs 背景图片 输出.json` 生成，已有人工编辑项目不要重建，保留文字和锁定项。

运行 `python3 scripts/serve.py` 打开本地编辑器；具体步骤和限制见编辑器说明。不自动发布到互联网或上传用户后续内容。本 Skill 仓库公开可见，但不授予第三方参考图片的再分发或商业使用许可。用户后续内容仍默认本地保存，不因仓库公开而自动上传。
